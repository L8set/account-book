import { PageTemplate } from '@component/template/PageTemplate'
import { FC, ReactNode } from 'react'

export const PurchaseHistory: FC = () => {
  const content: ReactNode = <div></div>
  return <PageTemplate title="購入履歴" content={content} />
}
